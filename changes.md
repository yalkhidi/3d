added free(resources→texpath) in clean resources();

added this 		clean_resources(resources);
in check warning function when faliures

free_gnl in parse_elements()

clean_and_print_error() in check_Warnings

